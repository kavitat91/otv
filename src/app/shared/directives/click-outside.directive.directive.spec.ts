import { ClickOutside.DirectiveDirective } from './click-outside.directive.directive';

describe('ClickOutside.DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickOutside.DirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
